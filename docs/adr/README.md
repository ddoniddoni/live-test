# Architecture Decision Records

Phase 0 does not make the PRD's decision-gated changes. Add an ADR here before changing
authentication, Socket.IO scaling, outbox/queue strategy, ORM, AI provider routing, data/role model,
or deployment topology.
